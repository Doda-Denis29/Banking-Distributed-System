package FrontEnd;

import javax.swing.*;

public interface GUI {
    public void createBasic(JFrame frame, float sizeX, float sizeY);
    public void endBasic(JFrame frame);
    public void createBasic(JFrame frame, float sizeX, float sizeY, ImageIcon icon);
}
